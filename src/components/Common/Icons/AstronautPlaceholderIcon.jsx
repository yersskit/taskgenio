import React from 'react';
import { FaUserAstronaut } from 'react-icons/fa';

const AstronautPlaceholderIcon = ({ ...rest }) => {
  return <FaUserAstronaut {...rest} />;
};

export default AstronautPlaceholderIcon;
