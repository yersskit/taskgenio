import React from 'react';
import { Item } from './Item';

const ItemDragOverlay = ({ id, content }) => {
  return <Item value={id + 'zdxvsdgv'} content={content} />;
};

export default ItemDragOverlay;
